# NANOLITH â†’ WordPress Mapping Note
One page, in case the client insists on WordPress/WooCommerce.
The current design system, copy and assets transfer 1:1 â€” nothing is wasted.

## Component mapping

| Current static asset | WordPress equivalent |
|---|---|
| 6 HTML pages | 6 WP Pages, each with a custom page template |
| `nanolith.css` (tokens + components) | Child-theme stylesheet, unchanged |
| `nanolith.js` behaviors (nav, filters, stepper, form) | Enqueued child-theme script, unchanged |
| `PRODUCTS` array in `nanolith.js` | Custom Post Type `product` + ACF field group (specs, apps, order info) |
| Data-sheet print view | Per-product PDF attached in Media Library, linked from product template |
| `ARTICLES` store | Native WP Posts + 3 categories: Research / Company / Industry |
| Category filter chips | `category.php` template (server-side filtering â€” better SEO than JS) |
| Trust-strip counters | Theme Customizer options or an options page |
| Team members | CPT `team_member` with featured image (replaces monogram avatars) |
| Inquiry form | **WPForms Lite** or **Contact Form 7** + **Flamingo** for stored entries |
| Honeypot | Built into WPForms Lite / CF7 + Antispam plugin |
| Breadcrumbs, meta, JSON-LD | **Yoast SEO** or **Rank Math** (free tiers) |
| sitemap.xml / robots.txt | Auto-generated by Yoast/Rank Math â€” delete the static files |
| Newsletter / updates | None currently â€” not needed |

## WooCommerce?

Only if the client wants **online transactions**. The catalog is
quote-based (Request a Quote â†’ inquiry form), so plain WP + the CPT above is
lighter, faster and free of store maintenance. If e-commerce is later required,
WooCommerce maps onto the same `product` CPT with prices added.

## Migration order

1. Install WP + the **Blocksy** or **GeneratePress** free theme (clean base).
2. Create child theme; paste `nanolith.css` as-is; enqueue `nanolith.js`.
3. Build the 6 page templates from the existing HTML section by section
   (copy-paste friendly â€” class names are already semantic).
4. Register the CPTs (`product`, `team_member`) + ACF fields; migrate content.
5. Swap the inquiry form for WPForms/CF7 with the same 6 fields + honeypot.
6. Install SEO plugin, verify meta per page, let it regenerate sitemap/robots.

**Effort estimate:** 3â€“5 working days for a developer familiar with WP.
The visual result can be pixel-identical â€” every token, class and component
carries over untouched.